# 对话 85

**时间**: 2026-03-06 11:56

---

**用户**: requestAnimationFrame 方案也没解决，26章读到最后点27章仍不从第1节开始。

**Claude**: 放弃时序方案，改为分步重置策略：

1. 先关闭旧章（`expandedChapter = -1`）并立即重置 `panel.scrollTop = 0`
2. 第一个 `nextTick` 后展开新章（`expandedChapter = idx`）
3. 第二个 `nextTick` 后在干净的滚动状态下计算新章标题偏移并滚动

核心思路：从 scrollTop=0 的干净状态开始计算偏移，避免旧章深处滚动位置干扰。

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: isSwitching 分支改为先关闭旧章+重置 scrollTop，再分步展开新章
