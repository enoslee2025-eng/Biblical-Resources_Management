# 对话 11

**时间**: 2026-03-05 10:14

---

**用户**: 版本标签（"版本 1"）希望可以编辑命名

**Claude**: 将版本标签改为可点击编辑的方式：点击标签弹出输入框，可输入自定义版本名称。默认显示 `v1`，编辑后显示自定义名称。

**修改的文件**:
- `website/src/views/BibleEditor.vue`:
  - 导入 `Edit` 图标
  - 新增 `versionName`、`editingVersion`、`versionNameInput` 变量
  - 版本标签改为可点击 + popover 编辑
  - 新增 `saveVersionName()` 函数
  - `buildMetaJson()` 包含 `versionName`
  - 添加 `.version-tag` 样式
