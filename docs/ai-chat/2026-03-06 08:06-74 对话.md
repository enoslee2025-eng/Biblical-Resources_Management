# 对话 74

**时间**: 2026-03-06 11:18

---

**用户**: 点击章节展开时，应该舒服地置顶查看，调整滚动行为

**Claude**: 在 BibleImportPreview 的 `toggleChapter` 中添加了自动滚动逻辑：展开章节时，使用 `nextTick` 等待 DOM 更新，然后通过 `getBoundingClientRect` 精确计算章节标题在滚动面板中的位置，调用 `panel.scrollTo` 平滑滚动到顶部。

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: toggleChapter 新增 scrollTo 逻辑，import 添加 nextTick
