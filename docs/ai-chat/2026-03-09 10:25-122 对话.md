# 对话 122

**时间**: 2026-03-09 11:54

---

**用户**: 1) 阅览页面点击「阅览」应该看到当前章节内容；2) 人名标注应为黑色下划线；3) 标注保存后返回能继续查看。

**Claude**:

1. **阅览跳转修复**：编辑器「阅览」按钮改为传递当前 book/chapter query 参数；BibleRead 新增 chapter query 参数读取。编辑器「修改」→阅览→「修改」双向跳转都保持位置。

2. **黑色下划线**：人名标注样式从 `#409eff`（蓝色）改为 `#333`（黑色）。

3. **标注状态持久化**：人名标注开关状态保存到 localStorage（按资源 ID 区分），页面重新打开时自动恢复。加载章节内容时自动检查并应用标注。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 新增 `goToRead()` 函数、`restorePersonNameMark()` 函数，localStorage 持久化标注状态
- `website/src/views/BibleRead.vue`: 新增 chapter query 参数读取
- `website/src/components/RichTextEditor.vue`: 下划线颜色改为 `#333`
