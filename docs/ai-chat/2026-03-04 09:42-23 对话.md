# 对话 23

**时间**: 2026-03-04 14:00

---

**用户**: 点击章节选择器应该精准跳转到对应章节

**Claude**: 问题是切换章节后滚动位置没有重置。修复：
1. 新增 `verseAreaRef` 引用经文区域 DOM 元素
2. watch `currentChapterIndex` 时用 `nextTick` 重置 `scrollTop = 0`
3. 模板 verse-area 添加 `ref="verseAreaRef"`

**修改的文件**:
- `website/src/views/BibleRead.vue`: 新增 verseAreaRef、nextTick 导入；watch 中重置滚动位置；模板添加 ref
