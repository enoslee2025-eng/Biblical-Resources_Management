# 对话 24

**时间**: 2026-03-05 11:22

---

**用户**: 按钮之间的间隙要均衡

**Claude**: 将第二行按钮的布局从 `gap: 8px` 改为 `justify-content: space-evenly`，使按钮间距均匀分布。

**修改的文件**:
- `website/src/views/BibleRead.vue`: `.control-row-bottom` 改为 `justify-content: space-evenly`，移除固定 gap
