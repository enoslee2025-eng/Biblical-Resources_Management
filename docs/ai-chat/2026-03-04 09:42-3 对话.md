# 对话 3

**时间**: 2026-03-04 10:08

---

**用户**: 给我加入导入（txt/word/pdf/json，四个板块都需要）

**Claude**: 实现了导入和导出功能，四个资源页面（圣经译本、词典、圣经注释、素材库）工具栏新增「📥 导入」和「📤 导出」按钮。
- 导入：支持拖拽/点击上传 TXT、Word(.docx)、PDF、JSON，调用后端 API
- 导出：选格式下载，JSON 格式纯前端实现可立即使用，其余格式需后端

**修改的文件**:
- `website/src/components/ImportDialog.vue`: 新建，导入对话框（拖拽上传、文件预览、进度状态）
- `website/src/components/ExportDialog.vue`: 新建，导出对话框（格式选择、JSON纯前端下载）
- `website/src/api/resource.js`: 新增 importResource API 函数
- `website/src/views/ResourceList.vue`: 引入两个对话框组件，添加导入/导出按钮和事件处理
