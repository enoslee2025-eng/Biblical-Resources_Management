# 对话 168

**时间**: 2026-03-10 11:20

---

**用户**: 页面空白，控制台报错 `TypeError: _ctx.t is not a function` at BibleRead.vue:706

**Claude**: BibleRead.vue 中使用了 `t()` 国际化函数但未导入 `useI18n`。添加了 `import { useI18n } from 'vue-i18n'` 和 `const { t } = useI18n()` 修复。

**修改的文件**:
- `website/src/views/BibleRead.vue`: 添加 useI18n 导入和 t 函数声明
