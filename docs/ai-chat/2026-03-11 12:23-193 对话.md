# 对话 193

**时间**: 2026-03-11 15:54

---

**用户**: 按照圣经文件导出说明 JSON 规范，部署到导出功能中

**Claude**: 根据导出规范文件更新了 `exportFormat.js` 中的 `convertBibleToAppFormat` 函数，确保导出 JSON 完全符合主内圣经 APP 的数据结构：

1. **富文本片段解析**：经文中的标题（【...】格式）拆分为 `f:2` 标题片段 + `f:0` 正文片段，而非之前的单一片段
2. **简介章特殊处理**：简介章(cid=0)的 `t` 字段为空，内容拆分为标题(f=2)/副标题(f=4)/正文(f=0)片段
3. **`t` 字段组合**：正文经节的 `t` 字段包含"标题 正文"组合文本
4. **页码索引生成**：新增 `_buildPageIndex()` 函数，为 `oldtPage`/`newtPage` 生成按章递增的页码映射
5. **附录数据透传**：支持从 meta 中传入 appendixCatalogs/appendixMaps/appendixStyle/appendixScript
6. **类型校正**：`infos` 和 `page` 强制转为 Boolean 类型

**修改的文件**:
- `website/src/utils/exportFormat.js`: 重写 `_buildChapterContent` 支持富文本片段、新增 `_buildPageIndex`、更新元数据类型处理
