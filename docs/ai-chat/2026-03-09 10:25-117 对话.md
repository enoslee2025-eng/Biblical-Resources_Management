# 对话 117

**时间**: 2026-03-09 10:53

---

**用户**: 智能排版后，第6节只显示一个引号 `"`，内容缺失。

**Claude**: 句子拆分正则 `/[^。！？]+[。！？]+/` 没有把句末的闭合引号 `"` 归入当前句子，导致 `"` 被拆成独立片段并分配为单独的一节经文。修复：正则增加闭合引号匹配 `["\u201d\u300d）》]*`，并新增纯标点片段合并逻辑，确保孤立引号合并到相邻句子。

**修改的文件**:
- `website/src/utils/bibleVerseParser.js`: `splitMergedVerses` 修复句子拆分正则和纯标点合并
- `website/src/views/BibleEditor.vue`: `repairBookChapters` 和 `repairAllBooks` 同步修复
