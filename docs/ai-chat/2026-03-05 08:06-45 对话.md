# 对话 45

**时间**: 2026-03-05 12:22

---

**用户**: 滑动后左右两边章节没有对齐

**Claude**: 将按比例同步改为基于章节分隔符对齐的同步方式：找到源面板当前可见的章节分隔符和章内滚动进度，在目标面板对齐到相同章节的相同位置。

**修改的文件**:
- `website/src/views/BibleRead.vue`: 重写 syncScroll 函数，使用章节分隔符 DOM 位置计算对齐
