# 对话 131

**时间**: 2026-03-09 14:46

---

**用户**: 顶部标题栏（编辑译本 v1、历史版本、阅览、保存）和下方的 Tab 栏（经文编辑、基本信息）应该合并为一体。

**Claude**: 将顶部标题栏和 Tab 面板合并到同一个容器 `.editor-panel` 中，共享背景、边框和圆角，视觉上成为一个整体。

1. **HTML 结构**：新增 `.editor-panel` 容器包裹 `.editor-header` 和 `el-tabs`，移除原来分离的两段结构。
2. **CSS 调整**：`.editor-panel` 承担白色背景、边框和圆角；`.editor-tabs` 不再独立设置背景和边框；`.editor-header` 的 `margin-bottom` 设为 0，与 Tab 紧密衔接。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 模板结构重构（`.editor-panel` 包裹标题和 Tab）；CSS 样式调整
