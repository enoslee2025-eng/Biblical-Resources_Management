# 对话 25

**时间**: 2026-03-04 14:21

---

**用户**: 编辑页面左侧行号和右侧经文文本滚动不同步

**Claude**: 修复 BibleEditor.vue 中行号区与文本框的滚动同步：
1. 新增 `lineNumbersRef` 和 `versesTextareaRef` 引用
2. `setupScrollSync()` 函数：监听 textarea 的 scroll 事件，同步更新行号区的 scrollTop
3. `.line-numbers` 添加 `overflow: hidden` 防止独立滚动
4. `onMounted` 和 `watch(activeTab)` 中调用同步设置

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 新增 refs、setupScrollSync、scroll 同步逻辑；行号区 overflow:hidden
