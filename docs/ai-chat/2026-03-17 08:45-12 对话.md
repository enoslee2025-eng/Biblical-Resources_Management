# 对话 12

**时间**: 2026-03-17 10:52

---

**用户**: （延续上次对话）修复 .doc 文件导入失败的问题

**Claude**: 深入排查后发现：

1. **后端 .doc 解析实际可用**：用有效 Token 测试后端 `/private/api/resource/parse-doc` 接口，成功解析了《太》注释测试.doc 文件
2. **前端回退逻辑有缺陷**：mammoth 解析 .doc 文件时可能返回空内容而不是抛出异常，导致不会走后端 POI 回退路径
3. **修复了三个文件**：
   - ImportDialog.vue：改进 .doc 解析逻辑，mammoth 返回空内容时也会回退到后端 POI 解析
   - CommentaryImport.vue：同样修复 mammoth 空结果不回退的问题
   - ResourceList.vue：注释资源的「导入」按钮现在跳转到专用的智能导入页面（支持经文引用解析）

**修改的文件**:
- `website/src/components/ImportDialog.vue`: 修复 .doc 解析回退逻辑
- `website/src/views/CommentaryImport.vue`: 修复 .doc 空结果回退逻辑
- `website/src/views/ResourceList.vue`: 注释导入跳转到专用智能导入页
