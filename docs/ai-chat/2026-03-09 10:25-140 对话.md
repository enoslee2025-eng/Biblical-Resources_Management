# 对话 140

**时间**: 2026-03-09 15:22

---

**用户**: 历史版本、阅览、保存这几个按钮样式统一，保存不要黑色底

**Claude**: 将「保存」按钮从黑色背景按钮改为与「历史版本」「阅览」一致的文字链接样式（`.header-link`），三个操作现在视觉统一。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 保存按钮改为 `<a class="header-link">` 文字链接，移除 `.save-btn` 黑色背景样式
