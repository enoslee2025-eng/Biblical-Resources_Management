# 对话 42

**时间**: 2026-03-05 12:10

---

**用户**: 对照模式右侧面板要有章节分隔和下滑加载，左右功能一致

**Claude**: 让右侧对照面板与左侧完全一致：
- 新增 `compareLoadedChaptersData` computed，与左侧共享相同的章节加载范围
- 右侧模板使用章节分隔符（第X章），支持经典/段落/大字三种显示模式
- 新增 `compareAreaRef`、`onCompareAreaScroll`，左右面板按比例同步滚动

**修改的文件**:
- `website/src/views/BibleRead.vue`:
  - 新增 `compareAreaRef`、`isSyncingScroll` 变量
  - 新增 `compareLoadedChaptersData` computed
  - 新增 `onCompareAreaScroll`、`syncScroll` 函数
  - 右侧对照面板模板改为使用 `compareLoadedChaptersData` + 章节分隔符
