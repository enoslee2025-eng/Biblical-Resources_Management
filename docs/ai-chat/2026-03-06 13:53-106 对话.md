# 对话 106

**时间**: 2026-03-06 14:45

---

**用户**: 按照详细规格重构整个智能排版系统（15项要求），包括新10步流程架构、Canonical Data Structure、删除旧逻辑、单元测试。

**Claude**: 完成整个 Scripture Parsing Pipeline 重构：

### 重构内容

**1. 重写 bibleVerseParser.js（新10步流程）**
- Step 1: Source Text Acquisition（由调用方提供）
- Step 2: Text Normalization（标准化文本）
- Step 3: Verse Candidate Detection（智能节号候选识别，排除 chapter:verse 引用）
- Step 4: Inline Verse Segmentation（支持行首/行中/紧贴三种节号模式）
- Step 5: Continuation Line Assignment（无节号行归入上一节）
- Step 6: Non-Scripture Filtering（过滤交叉引用/脚注/罗马数字/注释）
- Step 7: Punctuation Restoration（句尾补句号）
- Step 8: Speech Pattern Detection（对话结构检测，预留不自动修改）
- Step 9: Structural Validation（校验节号连续性/空经文/残留节号/引用混入）
- Step 10: Canonical Verse Structure（输出 { verse, text, needsManualReview }）

**2. 重写 autoFormatChapter()（新架构）**
- 优先使用 sourceImportText，避免从 verse 列表反拼
- 调用 parseBibleText 统一执行 Steps 2-9
- 直接保存 Canonical Data：text 不含节号

**3. 删除旧逻辑**
- 删除 stripVerseNum()
- 删除 regex 三轮换行预处理
- 删除 fallback 句子拆分
- 删除 text 字段节号前缀写入

**4. 更新所有引用**
- BibleEditor.vue: 模板/富文本/内联编辑器直接使用 v.text（无需 strip）
- confirmSmartFormat: 不再 prepend 节号
- confirmImport: 保存 sourceImportText
- bibleFormat.js: 输出 text 不含节号

**5. 单元测试（24个，全部通过）**
- Genesis 1 作为测试案例
- 覆盖：核心原则、Verse Candidate Detection、Inline Segmentation、Continuation Assignment、Non-Scripture Filtering、Punctuation Restoration、Structural Validation、Verse Range Expansion、Canonical Data Structure

### 核心原则
1. 不修改原始导入文本
2. text 字段永远不存储节号
3. verse number 只存在结构字段
4. parser 支持 inline verse detection
5. fallback 不生成假 verse
6. 非经文内容必须被过滤

**修改的文件**:
- `website/src/utils/bibleVerseParser.js`: 完全重写，新10步 pipeline
- `website/src/views/BibleEditor.vue`: 重写 autoFormatChapter、删除 stripVerseNum、更新所有引用
- `website/src/utils/bibleFormat.js`: text 不含节号
- `website/src/utils/__tests__/bibleVerseParser.test.js`: 新建，24个测试
- `website/package.json`: 添加 vitest + test 脚本
