# 对话 23

**时间**: 2026-03-17 15:51

---

**用户**: 注释模块不应该按圣经译本的逐节模式，而是要智能分析导入内容，按段落和主题进行整理分段

**Claude**: 提出注释模块重新设计方案：

1. **详情页**：不再显示66卷覆盖率，改为显示「目录大纲」，自动从内容中提取章节目录
2. **阅读页**：像书一样按章节/段落顺序阅读，左侧目录导航
3. **编辑页**：每个条目改为「标题 + 内容」（不再需要经文引用），支持拖拽排序
4. **导入智能分析**：自动按段落分割、提取标题，形成结构化章节列表

数据格式从 `[{verse, content}]` 改为 `[{title, content}]`

等待用户确认后开始编码。

**修改的文件**:
- 无（等待确认中）
