# 对话 26

**时间**: 2026-03-05 11:30

---

**用户**:
1. 文字颜色修改太专业了，要给常用颜色
2. 编辑页的「返回」改成「阅读」

**Claude**:
1. 将 RichTextEditor 的原生颜色选择器（color picker）替换为常用颜色面板：16 个文字颜色 + 12 个高亮颜色 + 清除高亮按钮，点击直接选色。
2. 编辑页面的「返回」按钮文字改为「阅读」。

**修改的文件**:
- `website/src/components/RichTextEditor.vue`:
  - 模板：`<input type="color">` 替换为颜色面板弹出层（`.color-palette`），包含预设色块网格
  - 脚本：新增 `showColorPicker`、`showHighlightPicker` 状态，`textColors`、`highlightColors` 预设数组，`clearHighlight()` 函数，点击外部关闭面板
  - CSS：新增 `.tb-color-dropdown`、`.color-palette`、`.color-palette-grid`、`.color-swatch`、`.color-clear-btn` 样式
- `website/src/views/BibleEditor.vue`: 「返回」→「阅读」
