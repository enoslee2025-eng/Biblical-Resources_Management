# 对话 8

**时间**: 2026-03-05 10:06

---

**用户**: 阅读页面第一章也要显示章标题（"第1章"），跟第2章一样，蓝色字体。

**Claude**: 移除了章标题的 `v-if="chData.chapterIndex > loadedStartIndex"` 条件，使所有章（包括第一章）都显示蓝色章标题。第一章标题没有顶部分隔线（`border-top: none`），后续章节保持原有的分隔线样式。

**修改的文件**:
- `website/src/views/BibleRead.vue`: 三种模式（经典、段落、大字）的章标题都改为始终显示；新增 `.chapter-divider-first` 样式（无顶部边线）
