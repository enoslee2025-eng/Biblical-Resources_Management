# 对话 35

**时间**: 2026-03-05 12:10

---

**用户**: 编辑模式回档到之前的样子

**Claude**: 撤销对话 20（自动选中第一节）和对话 21（共享编辑器布局）的所有改动：
- 恢复 `.font-toolbar` 节数统计栏（替换 `.inline-top-section`）
- 恢复每节独立 RichTextEditor（替换纯文本 + 共享编辑器）
- 移除所有自动选中第一节的逻辑（watcher、onSelectBook、onSelectChapter）
- 恢复 `.iv-edit-wrap` CSS，移除 `.inline-top-section`、`.inline-top-info`、`.editing-hint`、`.iv-text-active` CSS

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 模板、CSS 恢复到对话 18 的状态
