# 对话 6

**时间**: 2026-03-02 12:26

---

**用户**: 登录页两个按钮上看不见文字（截图显示白色按钮在紫色背景上，文字不可见）

**Claude**: 原因是 Vant Button 的 `plain` + `color="#fff"` 导致按钮背景变白、文字也是白色。修复为使用自定义 CSS 类：透明背景 + 白色文字 + 半透明白色边框。

**修改的文件**:
- `website/src/views/Login.vue`: 移除 plain 和 color="#fff"，改用自定义 .login-action-btn 样式类
