# 对话 59

**时间**: 2026-03-06 09:37

---

**用户**: 提供详细的圣经经文排版引擎规范，要求完全重写 bibleFormat.js，严格按照9大规则实现 formatBibleText 函数

**Claude**:
- 完全重写 `bibleFormat.js`，按照用户规范实现7步处理流程
- 新增节号保护机制：`isInsideQuote()` 检测引号内的数字，`isAfterClosingQuote()` 检测右引号后紧跟的数字（如"要有光！"2就有了光），这些数字不会被误识别为节号
- 处理流程：清理空格→统一标点→修复引号→识别节号（带保护）→拆分经文→句子修复→生成阅读格式
- 导出 `formatBibleText` 为主函数名，同时保留 `formatBibleForApp` 和 `formatVersesForApp` 兼容旧代码

**修改的文件**:
- `website/src/utils/bibleFormat.js`: 完全重写，实现圣经经文排版引擎
