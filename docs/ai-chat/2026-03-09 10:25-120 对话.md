# 对话 120

**时间**: 2026-03-09 11:46

---

**用户**: 点击人名标注按钮没有反应。

**Claude**: TipTap 编辑器不认识自定义的 `<span class="person-name">` HTML 标签，在 `setContent` 时自动剥离了。修复：创建自定义 TipTap Mark 扩展 `PersonName`，让编辑器能识别并保留此标签。

**修改的文件**:
- `website/src/components/RichTextEditor.vue`: 新增 `PersonName` Mark 扩展（基于 `@tiptap/core` 的 `Mark.create`），并注册到编辑器 extensions 列表中
