# 对话 132

**时间**: 2026-03-09 14:54

---

**用户**: 整体布局太乱，希望简约风格但功能齐全；按钮需要保留文字而不是纯图标。

**Claude**: 重新设计了编辑页面布局，更简约紧凑：

1. **一体化面板**：标题栏、Tab、工具栏在同一个白色面板内
2. **精简工具栏**：书卷/章节选择器 + 操作按钮 + 编辑模式切换合并为一行，用竖线分隔器分组
3. **按钮保留文字**：导入、智能排版、人名标注、清除本章仍显示文字，使用 `size="small"` 缩小尺寸
4. **移除冗余标签**：去掉"书卷:"和"章节:"的前缀文字
5. **移除重复章标题**：编辑器内不再显示"创世记 第8章"标题（工具栏选择器已表示）
6. **历史版本和阅览按钮改为 text 样式**：更轻量

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 模板结构重构 + CSS 新增 `.toolbar` 系列样式
