# 对话 8

**时间**: 2026-03-03 10:15

---

**用户**: 新增译本保存后，在列表中看不到

**Claude**: 找到问题原因：API 响应拦截器已经自动解包了 `response.data`，返回的是 `{ code: 200, data: {...} }`，但页面代码又做了一层 `{ data }` 解构，导致读取到的是内层数据，`data.code` 变成了 `undefined`，列表数据永远无法正确加载。

修复了所有受影响的文件中的数据访问模式。

**修改的文件**:
- `website/src/views/ResourceList.vue`: 修复 `loadResources` 数据读取
- `website/src/views/BibleDetail.vue`: 修复 `loadDetail` 数据读取
- `website/src/views/BibleRead.vue`: 修复 `loadData` 数据读取
- `website/src/views/BibleEditor.vue`: 改进保存错误提示，显示具体错误信息
- `website/src/views/UserManagement.vue`: 修复 `loadUsers` 数据读取
- `website/src/views/LoginNew.vue`: 修复登录响应数据读取
