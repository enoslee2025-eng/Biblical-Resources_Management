# 对话 30

**时间**: 2026-03-05 11:48

---

**用户**: 回档到对话 27 的状态（阅览按钮）

**Claude**: 将「阅览」按钮回退到对话 27 的状态：`<el-button @click="router.back()">阅览</el-button>`，移除了对话 29 添加的 `v-if="isEdit"` 和 `router.push` 跳转逻辑。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 阅览按钮回退为 `router.back()` + 无条件显示
