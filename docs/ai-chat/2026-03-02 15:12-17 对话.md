# 对话 17

**时间**: 2026-03-02 17:48

---

**用户**: 现在网站回不到主界面了，先修复一下

**Claude**: 将所有编辑器页面的返回按钮从 `router.back()`（依赖浏览器历史）改为 `router.push('/')`（直接跳转首页），确保任何情况下都能回到主页。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 返回按钮改为 router.push('/')
- `website/src/views/CommentaryEditor.vue`: 同上
- `website/src/views/DictionaryEditor.vue`: 同上
- `website/src/views/BibleExport.vue`: 同上
