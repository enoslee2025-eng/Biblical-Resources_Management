# 对话 4

**时间**: 2026-03-04 10:21

---

**用户**: 导入时报 500 错误，请修复

**Claude**: 问题根因：前端直接把原始文件发给后端，后端接口未实现所以 500。
修复方案：改为前端本地解析文件内容（mammoth 解析 docx，pdfjs-dist 解析 PDF，FileReader 读 txt/json），再把解析好的文字内容发给后端 /private/api/resource/import-content 接口。同时修复了 PDF worker 加载方式，改用 Vite 的 `?url` 语法。

**修改的文件**:
- `website/src/components/ImportDialog.vue`: 改为前端本地解析文件，修复 PDF worker 路径
