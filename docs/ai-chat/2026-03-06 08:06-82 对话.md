# 对话 82

**时间**: 2026-03-06 11:48

---

**用户**: 点开5章查看完没有关闭，直接点开12章，结果节并不是从第一节开始的，需要修复。

**Claude**: 在 `toggleChapter` 中新增"切换章"分支（`isSwitching`）：

1. **检测切换**：当前已有展开的章，且点击的不是同一章 → 走切换逻辑
2. **不覆盖 savedScrollTop**：保留最初展开前的滚动位置，关闭时仍能回到原位
3. **先更新状态再定位**：`expandedChapter = idx` 后在 `nextTick` 中重新获取新章 header，计算正确偏移并滚动

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: toggleChapter 新增 isSwitching 分支，切换章时不覆盖 savedScrollTop，正确滚动到新章标题
