# 对话 4

**时间**: 2026-03-05 09:20

---

**用户**: 编辑页面下面灰色的经文逐节编辑区域不需要同时出现，只要文本编辑。工具栏不够专业美观，要参考 Office/WPS 风格重新设计。

**Claude**: 修复了两个问题：

1. **编辑器互斥显示 bug**：模板中 `v-else` 错误地与 `v-if="editorViewMode === 'inline'"` 配对，导致选择「文档编辑」时文本编辑器同时出现。改为 `v-if="editorViewMode === 'raw'"`。

2. **工具栏重新设计**：参照 Office/WPS 专业风格完全重写 RichTextEditor 组件：
   - 使用 SVG 图标代替文字字符（加粗、斜体、对齐等）
   - 新增字体选择下拉框（宋体、楷体、黑体、微软雅黑、Arial 等）
   - 新增字号选择下拉框（12-36px）
   - 新增删除线、高亮颜色功能
   - 新增两端对齐按钮
   - 工具栏采用 Office 风格渐变背景和按钮样式
   - 安装了 @tiptap/extension-highlight 和 @tiptap/extension-font-family 扩展

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 修复 v-else → v-if="editorViewMode === 'raw'"
- `website/src/components/RichTextEditor.vue`: 完全重写为 Office/WPS 风格工具栏
