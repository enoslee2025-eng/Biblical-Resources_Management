# 对话 19

**时间**: 2026-03-04 12:30

---

**用户**: PDF 页码标记（"创世记 2:1 2 创世记 3:7"）被误当经文，章节标题（如"制定安息日"）要保留

**Claude**: 三项修复：
1. 新增 `cleanBiblePageHeaders()` 函数：移除 PDF 页眉（"书名 章:节" 格式行、多引用组合行）
2. `splitTextToVerses` 增加段落标题识别：首节前的短文本 + 经文末尾短文本 → verse: 0
3. `_mergeStandaloneNumbers` 也增加标题识别
4. 新增辅助函数 `_looksLikeHeading()`、`_extractTrailingHeading()`
5. `BibleRead.vue`：verse.number 用 `??` 替代 `||` 支持 0 值；三种显示模式均支持 section-heading 样式
6. `parseFullBibleText` 调用 `cleanBiblePageHeaders` 预处理文本

**修改的文件**:
- `website/src/utils/fileImport.js`: 新增 cleanBiblePageHeaders、_looksLikeHeading、_extractTrailingHeading；更新 splitTextToVerses 策略0、_mergeStandaloneNumbers、parseFullBibleText
- `website/src/views/BibleRead.vue`: verse number 用 ?? 运算符；三种模式支持 section-heading；新增 CSS 样式
