# 对话 112

**时间**: 2026-03-06 16:32

---

**用户**: 「整本圣经」导入模式应该也支持选择文件，不仅仅是文件夹。

**Claude**: 在「整本圣经」导入模式新增了「选择文件」按钮，支持选择多个 .txt/.pdf/.docx/.json 文件，与文件夹选择并列。文件选择复用了与文件夹选择相同的处理逻辑（书卷名匹配、智能分章等）。

**修改的文件**:
- `website/src/views/BibleEditor.vue`:
  - 新增 `fullBibleFileInput` ref 和 `onFullBibleFileSelected()` 函数
  - 模板中「整本圣经」区域新增「选择文件」按钮（支持多选）
