# 对话 21

**时间**: 2026-03-17 14:11

---

**用户**: 导入后，还是没有任何显示呢（注释资源导入后书卷目录全部为空，0/66覆盖）

**Claude**: 修复了注释资源导入后无法显示的问题。根本原因：导入注释时没有智能解析，原始文本被后端包装为 `{"text":"..."}` 对象格式，但注释详情页期望的是条目数组格式。

修复内容：
1. ImportDialog 新增注释智能解析：导入时自动识别文本中的经文引用（如创1:1），按引用切分为结构化条目数组
2. fileImport 新增两个函数：parseCommentaryText（文本解析）和 commentaryJsonToEntries（APP JSON格式转换）
3. CommentaryDetail 添加兼容处理：遇到旧格式 `{"text":"..."}` 时自动转换为条目数组

**修改的文件**:
- `website/src/components/ImportDialog.vue`: 注释和词典资源导入时添加智能解析逻辑
- `website/src/utils/fileImport.js`: 新增 parseCommentaryText、commentaryJsonToEntries 函数
- `website/src/views/CommentaryDetail.vue`: 兼容旧格式自动转换
