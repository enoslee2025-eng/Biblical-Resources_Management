# 对话 33

**时间**: 2026-03-05 12:00

---

**用户**: 对照模式下右侧译本没有应用相同的显示模式，要求左右一致

**Claude**: 修复了对照模式右侧面板的显示模式。原来右侧硬编码为 `mode-classic`，只有经典模式的渲染。现在右侧动态使用 `displayMode`，支持经典/段落/大字三种模式，与左侧保持一致。新增 `compareParagraphs` computed 用于右侧段落模式分组。

**修改的文件**:
- `website/src/views/BibleRead.vue`:
  - 右侧对照面板：`mode-classic` → `'mode-' + displayMode`
  - 经文渲染：单一 `verse-classic` → 三种模式（classic/paragraph/large）条件渲染
  - 经典模式标签从 `<p>` + `<sup>` 改为 `<div>` + `<span>` 与左侧一致
  - 新增 `compareParagraphs` computed（每 5 节分一段）
