# 对话 186

**时间**: 2026-03-11 11:53

---

**用户**: 注释的智能上传不支持 Word (.doc) 格式

**Claude**: 所有三个导入页面（圣经、注释、词典）的 Word 导入都只支持 .docx，不支持旧版 .doc。已全部修复：文件验证逻辑添加 .doc 后缀检查，file input accept 属性添加 .doc，格式标签改为 "DOC / DOCX"。

**修改的文件**:
- `website/src/views/CommentaryImport.vue`: 添加 .doc 支持（验证 + accept + 格式标签）
- `website/src/views/DictionaryImport.vue`: 同上
- `website/src/views/BibleImport.vue`: 同上
