# 对话 121

**时间**: 2026-03-09 11:50

---

**用户**: 在马太福音的阅览界面点击「修改」后，编辑器跳转到了创世记而不是马太福音。

**Claude**: 阅览页的「修改」按钮跳转时没有传递当前书卷和章节信息，编辑器默认从创世记第1章开始。修复：在跳转 URL 中添加 `book` 和 `chapter` query 参数，编辑器 `onMounted` 中读取这些参数并定位到对应位置。

**修改的文件**:
- `website/src/views/BibleRead.vue`: 「修改」按钮跳转时传递 `book` 和 `chapter` query 参数
- `website/src/views/BibleEditor.vue`: `onMounted` 中读取 query 参数恢复书卷和章节位置
