# 对话 23

**时间**: 2026-03-05 11:18

---

**用户**: 阅读页面的按钮行左右布局对齐，不要空区域不对称

**Claude**: 将控制栏从单行 flex-wrap 布局改为双行独立布局。第一行（control-row-top）：书卷/章节选择 + 上一章/下一章导航。第二行（control-row-bottom）：显示模式、字体、对照、修改按钮，左对齐无空白。

**修改的文件**:
- `website/src/views/BibleRead.vue`:
  - 模板：`.control-left` → `.control-row-top`，`.control-right` → `.control-row-bottom`
  - CSS：`.control-bar` 改为 `flex-direction: column`，两行各自 `display: flex` 左对齐
