# 对话 14

**时间**: 2026-03-05 10:24

---

**用户**:
1. 版本历史面板添加删除单个版本和清空所有历史的功能
2. 进度概览和书卷/章节选择器合并为一行

**Claude**:
1. 版本历史：每个版本卡片新增「删除」按钮，面板标题旁新增「清空历史」按钮，带确认弹窗
2. 进度和选择器合并：去掉单独的进度条行，将进度信息嵌入选择器行中

**修改的文件**:
- `website/src/components/VersionHistory.vue`: 新增 `handleDelete`、`handleClearAll` 函数，模板添加删除按钮和清空按钮，header 布局调整
- `website/src/api/resource.js`: 新增 `deleteVersion`、`clearVersions` API
- `website/src/i18n/zh.js`: 新增版本删除/清空相关 i18n 键
- `website/src/i18n/en.js`: 同上英文翻译
- `website/src/views/BibleEditor.vue`: 进度概览和选择器合并为一行
