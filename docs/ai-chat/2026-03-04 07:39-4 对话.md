# 对话 4

**时间**: 2026-03-04 08:20

---

**用户**: 点击"管理员直接进入"一直停留在登录页面，没有跳转

**Claude**: 原因是之前设了假 token `admin-temp-token`，后端返回 401 后拦截器清除 token 并跳回登录页。改为通过后端 QR 流程获取真实 Token（createQr → devConfirmQr → getQrStatus），拿到有效 token 后跳转。

**修改的文件**:
- `website/src/views/LoginNew.vue`: quickAdminLogin 改为调用真实后端接口获取有效 token
