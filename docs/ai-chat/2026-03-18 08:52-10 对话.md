# 对话 10

**时间**: 2026-03-18 11:39

---

**用户**: 目录大纲不应该以经文引用来划分段落，经文跟整体目录没有关系，划分不清晰。

**Claude**: 修改目录导航逻辑，只保留真正的文档结构层级作为导航项：
- 保留：文档标题、作者、序言、章标题、单元标题、小节标题
- 移除：经文引用(verse_ref)、经文引用块(scripture_block)、编号注释(numbered_note)
- 移除：从正文中提取经文引用作为导航标签的逻辑

**修改的文件**:
- `website/src/views/CommentaryRead.vue`: 修改 navItems 计算属性，navTypes 只保留结构类型
