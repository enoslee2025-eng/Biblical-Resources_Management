# 对话 84

**时间**: 2026-03-06 11:54

---

**用户**: 双重 nextTick 没有解决切换章不从第1节显示的问题。

**Claude**: 改用 `nextTick` + `requestAnimationFrame` 组合：nextTick 让 Vue 完成 DOM 更新，requestAnimationFrame 等浏览器完成布局重排后再计算章标题偏移并滚动。

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: isSwitching 分支改为 nextTick + requestAnimationFrame
