# 对话 137

**时间**: 2026-03-09 15:13

---

**用户**: 多个需求合并处理：
1. 「人名标注」改为「智能标注」下拉菜单，包含人名标注、地名标注
2. 按钮颜色和样式统一
3. 左上角 logo 改为竖排样式（圣经 + Bible Pro 上下排列）
4. 数据中心4个板块各自添加 AI 导入入口（圣经AI导入、AI词典导入、AI注释导入、AI素材导入）

**Claude**:

1. **智能标注下拉菜单**：新建 `biblePlaceNames.js` 地名数据库（旧约+新约主要地名），提供标注/清除函数。编辑器中「人名标注」按钮改为「智能标注」下拉菜单，点击展开可勾选「人名标注」和「地名标注」。重构标注逻辑为统一的 `rebuildAllMarkedText()` 函数。

2. **Logo 竖排**：MainLayout.vue 的 logo 改为竖排布局（圣经在上、Bible Pro 徽章在下），加灰色背景圆角卡片。

3. **4个AI导入入口**：DataCenter.vue 的单个导入卡片改为 2×2 网格，4个卡片颜色分别对应4个板块主题色（蓝/橙/绿/紫）。

4. **阅览页地名样式**：BibleRead.vue 新增 `.place-name` 蓝色波浪下划线样式。

**修改的文件**:
- `website/src/utils/biblePlaceNames.js`: 新建，地名数据库 + 标注函数
- `website/src/views/BibleEditor.vue`: 智能标注下拉菜单 + 标注逻辑重构
- `website/src/views/BibleRead.vue`: 地名标注 CSS
- `website/src/views/DataCenter.vue`: 4个AI导入入口卡片
- `website/src/layouts/MainLayout.vue`: Logo 竖排样式
- `website/src/i18n/zh.js`: 新增智能标注、导入相关翻译
- `website/src/i18n/en.js`: 同上英文翻译
