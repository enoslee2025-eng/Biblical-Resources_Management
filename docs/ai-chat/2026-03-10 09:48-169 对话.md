# 对话 169

**时间**: 2026-03-10 11:22

---

**用户**: 页面仍然白屏

**Claude**: 发现 BibleRead.vue 的 `isBookFilled(books[idx-1])` 在 books 未加载时传入 undefined 导致崩溃。修复：1) isBookFilled 加 null 检查；2) 旧约用 Math.min(39, books.length) 限制循环；3) 新约分组加 v-if="books.length > 39"。

**修改的文件**:
- `website/src/views/BibleRead.vue`: 修复 isBookFilled 空值崩溃 + 书卷选择器安全渲染
