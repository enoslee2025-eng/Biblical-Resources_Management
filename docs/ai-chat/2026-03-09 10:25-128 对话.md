# 对话 128

**时间**: 2026-03-09 14:20

---

**用户**: 节号和经文内容要紧挨着；耶和华的名字不需要自动下划线标注。

**Claude**:

1. **节号紧贴经文**：将 `.verse-num` 和 `.verse-num-large` 的 `padding-right: 4px` 改为 `margin-right: 2px`，节号与经文几乎紧挨。

2. **移除耶和华下划线**：从 `biblePersonNames.js` 的人名列表中移除了 `'耶和华'` 和 `'主'`，人名标注功能不再对这两个称呼添加下划线。

**修改的文件**:
- `website/src/views/BibleRead.vue`: `.verse-num` 和 `.verse-num-large` 间距缩小至 2px
- `website/src/utils/biblePersonNames.js`: 移除 `'耶和华'` 和 `'主'`
