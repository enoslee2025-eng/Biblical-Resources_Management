# 对话 32

**时间**: 2026-03-05 11:55

---

**用户**: 逐节编辑里面改了，但文档编辑中没有任何改动，需要修复数据同步

**Claude**: 重新实现三种编辑模式的数据同步：
- 离开 inline/raw 模式时：清除 `chapter.richText` 缓存，使文档模式从最新 `verses` 重新生成
- 离开 rich 模式时：调用 `syncRichTextToVerses()` 将富文本解析回 `verses`

**修改的文件**:
- `website/src/views/BibleEditor.vue`:
  - editorViewMode watcher：离开 inline/raw 时清除 richText，离开 rich 时调用 syncRichTextToVerses
  - 新增 `syncRichTextToVerses()` 函数
