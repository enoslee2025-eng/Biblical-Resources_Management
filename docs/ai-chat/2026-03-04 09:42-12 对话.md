# 对话 12

**时间**: 2026-03-04 11:50

---

**用户**: 删除失败：HTTP 404 Not Found

**Claude**: 问题：前端显示的"马太福音"是 localStorage 本地资源（local_ ID），删除时误调了后端 API，后端找不到该 ID 所以 404。
修复：handleDelete 里先判断 isLocalId，是本地资源就调 deleteLocal，否则才调后端 deleteResource。

**修改的文件**:
- `website/src/views/ResourceList.vue`: handleDelete 加 isLocalId 判断，本地资源走 deleteLocal
