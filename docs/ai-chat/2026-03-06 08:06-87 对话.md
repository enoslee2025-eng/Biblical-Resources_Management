# 对话 87

**时间**: 2026-03-06 12:00

---

**用户**: "保存到数据库"和"导出JSON文件"两个按钮没有对齐。

**Claude**: 给 `.action-buttons` 添加 `align-items: stretch` 让两个按钮撑满相同宽度，并添加 `.el-button { margin-left: 0 }` 覆盖 Element Plus 默认的左边距，确保两个按钮完全对齐。

**修改的文件**:
- `website/src/views/BibleImportSave.vue`: action-buttons 添加 align-items: stretch 和 el-button margin-left: 0
