# 对话 9

**时间**: 2026-03-04 11:20

---

**用户**: 导入文档后没有加载文档内容，需要更智能化，自动识别章节和经文排版（按现有规范格式）

**Claude**: 在 fileImport.js 中添加了 splitTextToVerses、stripLineNumbers、buildBibleContentJson 等工具函数，并更新 ImportDialog.vue 在圣经译本导入时自动：
1. 从文件名识别书卷（如"太1-5.docx"→马太福音）
2. 从内容识别书卷（检测马太福音、太等关键词）
3. 用 splitIntoChapters 按章分割（支持多种格式）
4. 用 splitTextToVerses 把每章解析成 {verse, text} 数组
5. 构建完整的 66 卷标准数据结构存入 contentJson
后端同步更新：直接接收前端解析好的 contentJson，不再二次处理。

**修改的文件**:
- `website/src/utils/fileImport.js`: 新增 splitTextToVerses、stripLineNumbers、buildBibleContentJson、initEmptyBibleBooks、BIBLE_BOOK_NAMES、BOOK_CHAPTER_COUNTS 导出
- `website/src/components/ImportDialog.vue`: 加入圣经智能解析（书卷识别 + 章节分割 + 经文结构化）
- `server/src/main/java/.../resource/dto/ImportContentRequest.java`: 新增 contentJson 字段
- `server/src/main/java/.../resource/resource/ResourceResource.java`: importContent 优先使用前端传入的 contentJson
