# 对话 49

**时间**: 2026-03-05 12:32

---

**用户**: 左侧标签要改成跟右侧一样的下拉选择框，可以切换译本

**Claude**:
- 左侧标签改为 el-select 下拉框，可选择译本
- 新增 `leftResourceId` ref 和 `allBibleList` ref
- `bibleList` 改为 computed，自动排除左侧当前译本
- 新增 watcher 监听 leftResourceId 变化，自动加载新译本数据
- loadBibleList 改为加载所有译本（不再排除当前）

**修改的文件**:
- `website/src/views/BibleRead.vue`:
  - 新增 allBibleList、leftResourceId 变量
  - bibleList 改为 computed
  - loadBibleList 加载全部译本
  - 新增 leftResourceId watcher
  - 左侧标签改为 el-select 下拉框
