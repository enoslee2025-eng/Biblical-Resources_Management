# 对话 194

**时间**: 2026-03-11 16:21

---

**用户**: 提供了创世记第2章的真实导出 JSON 数据，要求对照检查并继续完善导出格式

**Claude**: 对比真实数据和现有代码，发现并修复了以下差异：

1. **纯文本 `t` 字段**：新增 `_stripTags()` 函数，`t` 字段去除 `<u>`/`<i>`/`<r>`/`<m>`/`<l>` 等标签，`d.d` 保留富文本标签
2. **片段 `t` 字段是缩进级别**：修正注释说明，诗句使用 `t:2` 表示二级缩进
3. **诗歌/引语检测**：新增 `_isPoetry()` 函数，多行文本（`\n` 分隔）自动识别为诗句格式 `f:1`
4. **引语引导语处理**：如"那人说："作为 `f:0` 正文，后续诗行作为 `f:1` 诗句并带 `t:2` 缩进
5. **片段 `b` 字段更精确**：标题后的正文 `b:0`，诗行间 `b:1`，段落间 `b:2`

**修改的文件**:
- `website/src/utils/exportFormat.js`: 重写 `_buildChapterContent`，新增 `_stripTags`、`_isPoetry` 辅助函数
