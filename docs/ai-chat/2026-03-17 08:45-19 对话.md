# 对话 19

**时间**: 2026-03-17 13:53

---

**用户**: 继续上次对话（导入圣经PDF报500错误+内容为空的修复）

**Claude**: 完成了所有修复工作：

1. **前端 ImportDialog**：有结构化数据时不发送原始文本，减小请求体积避免500
2. **MySQL 配置**：docker-compose.yml 增加 `--max-allowed-packet=64M`
3. **后端错误处理**：importContent 接口添加 try-catch，返回友好错误提示
4. **PDF书卷检测增强**：
   - detectAllBooks 增加策略2（书卷名在行首后跟章节号，适配PDF提取格式）
   - 整本圣经模式阈值从3卷降低到2卷
   - parseFullBibleText 阈值从2降低到1（即使只检测到1卷也尝试解析）
   - 未识别到书卷时默认放入创世记（索引0），确保内容不丢失

**修改的文件**:
- `website/src/components/ImportDialog.vue`: 减小请求体积、放宽检测阈值、兜底默认创世记
- `website/src/utils/fileImport.js`: detectAllBooks 增加PDF行首匹配策略、parseFullBibleText 降低阈值
- `docker/docker-compose.yml`: MySQL 增加 max-allowed-packet=64M
- `server/src/.../resource/resource/ResourceResource.java`: importContent 添加异常捕获
