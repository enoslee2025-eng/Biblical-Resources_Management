# 对话 21

**时间**: 2026-03-04 13:56

---

**用户**: 段落标题（如 "2 制定安息日"）不是经文内容，自动删除掉。添加功能：经文章能够下滑持续阅读下一章。

**Claude**: 两项修改：
1. 移除 BibleRead.vue 中 section heading 显示代码（v-if="v.number === 0" 相关模板），因为标题已在导入时被丢弃
2. 新增连续滑动阅读功能：
   - 新增 `loadedChapterCount` 状态，追踪已加载的章数
   - 新增 `loadedChaptersData` 计算属性，返回从当前章开始的多章数据（含经文和段落分组）
   - 经文区域 `@scroll` 事件检测：接近底部时自动加载下一章
   - 三种显示模式（经典/段落/大字）均支持多章渲染，章间显示蓝色分隔标题
   - 底部显示 "↓ 继续滑动加载下一章" 提示
   - 切换书卷或章节时自动重置加载数量

**修改的文件**:
- `website/src/views/BibleRead.vue`: 移除 heading 显示、新增连续滑动阅读（loadedChaptersData/onVerseAreaScroll/chapter-divider 样式）
