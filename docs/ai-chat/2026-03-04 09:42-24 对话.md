# 对话 24

**时间**: 2026-03-04 14:05

---

**用户**: 滑动时，经文前面的小节数字应该跟着滑动对齐

**Claude**: 将经典模式和大字模式的经文布局从 inline `<sup>` 改为 flex 两列布局：
- 左列：节号（固定宽度，右对齐）
- 右列：经文正文（自适应宽度）
- 文字换行时不会跑到节号下方，始终保持对齐
- 段落模式保持 inline 节号不变

**修改的文件**:
- `website/src/views/BibleRead.vue`: 经典模式和大字模式改用 `<div>` + flex 布局；verse-num/verse-num-large 改为固定宽度左列；新增 .verse-text 样式
