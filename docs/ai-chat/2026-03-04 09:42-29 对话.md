# 对话 29

**时间**: 2026-03-04 14:45

---

**用户**: 把书卷简写自动填上；行号数字有的有阴影有的没有，需要统一

**Claude**: 两项修复：

1. **书卷简写**：新增 `bibleBookAbbrs` 数组（创、出、利、民、申...启），`initEmptyBooks` 自动填入简写；已有数据通过 `fillMissingAbbrs()` 在加载后补充缺失简写

2. **行号阴影统一**：为 `.line-num` 添加 `-webkit-font-smoothing: antialiased`、`text-shadow: none`、`font-weight: 400` 确保渲染一致；textarea 和外层包裹器强制 `box-shadow: none !important`

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 新增 bibleBookAbbrs 数组；initEmptyBooks 使用简写；新增 fillMissingAbbrs 函数；loadResource 三处加载点调用 fillMissingAbbrs；line-num CSS 统一字体渲染；textarea 消除阴影
