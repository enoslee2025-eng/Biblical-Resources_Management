# 对话 108

**时间**: 2026-03-06 15:29

---

**用户**: 智能排版后，一个 verse 条目仍然包含多节经文的合并文本（无内嵌节号），无法拆分为独立节。

**Claude**: 新增 `splitMergedVerses()` 函数，当解析结果的节数远少于预期（< 80%）时，按中文句尾标点（。！？）拆分长文本，根据 `BIBLE_VERSE_COUNTS` 标准数据将句子均匀分配到各节。

在 `autoFormatChapter()` 中，解析完成后调用 `splitMergedVerses(parsedChapter.verses, expectedCount)` 进行合并经文拆分。

**修改的文件**:
- `website/src/utils/bibleVerseParser.js`: 新增 `splitMergedVerses()` 导出函数，按句子边界拆分合并经文
- `website/src/views/BibleEditor.vue`: 导入 `splitMergedVerses`，在 `autoFormatChapter()` 解析后按 `BIBLE_VERSE_COUNTS` 拆分合并经文
