# 对话 8

**时间**: 2026-03-18 11:24

---

**用户**: 提出了一套完整的智能排版规范，共10大类规则：
1. 先做文档清洗再排版（删除PAGE、修复OCR断裂、合并断行）
2. 强制识别文档层级（封面页→序言→目录→正文总论→分章→小节→经文引用块→编号注释段）
3. 标题层级固定为4级（篇章级→章级→章内单元→主题小节）
4. 目录必须重建，不可照抄原断行
5. 正文段落规则（一段一个意思、160-200字自动拆段、引经论证结论不混塞）
6. 经文与引用专门处理（短引内联、多行独立成引用块）
7. 编号段落统一（①②③识别为编号注释段）
8. 宗教讲章类文档专门规则（主题句突出、小标题可扫读、讲解层次清楚）
9. 不要微调原文，要重建可读版式
10. 输出标准（一眼看出标题层级、目录独立整洁、经文有视觉区隔）

**Claude**: 全面重写智能排版系统，实现所有10大类规则：

1. **新增 cleanText() 文本清洗函数**：删除PAGE标记和孤立页码、修复断裂标点和括号、统一经文冒号格式、合并被错误断开的短行、清理多余空白
2. **增强 reAnalyzeSections()**：
   - 第一步文本清洗所有条目
   - 第二步合并断裂标题（相邻短标题无内容→合并）
   - 第三步目录合并
   - 第四步层级识别：新增 unit_title(章内单元)、numbered_note(①②③编号段)、scripture_block(多行经文引用块) 三种块类型
   - 正文中埋藏的小节标题自动提取
3. **增强 formatContent()**：文本清洗管线→编号段识别→多行经文引用块识别→长段落自动拆分→经文高亮
4. **目录重建**：新增 rebuildTocContent() 函数，规范化每项格式，按章/节/经文分层缩进
5. **4级标题CSS**：h1(22px/700)→h2(18px/700)→h3(16px/600)→h4(15px/600)
6. **新增CSS**：多行经文引用块（左侧金色竖线）、编号注释段（编号高亮、不缩进）、目录分层缩进
7. **8语言i18n更新**：新增 block_type_unit、block_type_scripture、block_type_numbered

**修改的文件**:
- `website/src/views/CommentaryRead.vue`: 新增cleanText、rebuildTocContent函数，重写reAnalyzeSections和formatContent，新增3种块类型模板和CSS
- `website/src/i18n/zh.js`: 新增3个块类型翻译
- `website/src/i18n/en.js`: 同上
- `website/src/i18n/ja.js`: 同上
- `website/src/i18n/ko.js`: 同上
- `website/src/i18n/th.js`: 同上
- `website/src/i18n/el.js`: 同上
- `website/src/i18n/he.js`: 同上
- `website/src/i18n/my.js`: 同上
