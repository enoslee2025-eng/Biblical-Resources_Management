# 对话 107

**时间**: 2026-03-06 15:00

---

**用户**: 智能排版报错"排版后无有效经文"，无法排版。

**Claude**: 新架构中 text 字段不再包含节号，但 `autoFormatChapter` 的 fallback（从 verse 列表重建 rawText）直接用 `v.text` 拼接，导致 rawText 中没有任何节号标记，parser 无法识别任何 verse，返回空结果。

修复：当没有 `sourceImportText` 时，从 verse 列表重建 rawText 时 prepend `v.verse`（仅用于临时重建供 parser 识别，不存入 text 字段）。同时检测旧数据格式（text 已含节号前缀）避免重复添加。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 修复 autoFormatChapter 的 rawText fallback，prepend verse number 供 parser 识别
