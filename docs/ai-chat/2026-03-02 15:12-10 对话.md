# 对话 10

**时间**: 2026-03-02 16:32

---

**用户**: 选择文件夹导入时，默认为按整卷书导入

**Claude**: 在 `BibleEditor.vue` 的 `onFolderSelected` 函数中，文件夹选择后自动将 `importScope` 设为 `'book'`（整卷书模式）。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 文件夹导入时自动切换为整卷书导入模式
