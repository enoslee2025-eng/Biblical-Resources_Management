# 对话 77

**时间**: 2026-03-06 11:32

---

**用户**: 滑动经文后再点击章标题，位置会错乱（sticky 导致章标题跟着节一起滑动）。而且经文之间有不必要的间隙。

**Claude**: 修复了两个 UI 问题：

1. **去掉 sticky 定位**：移除 `.chapter-header` 的 `position: sticky`，改为给 `.verse-list` 设置 `max-height: 60vh; overflow-y: auto`，让经文在固定高度区域内独立滚动，章标题列表保持不动
2. **去掉经文间隙**：移除 `.verse-item + .verse-item` 的 `border-top`，减小 padding 从 `5px` 到 `3px`
3. **简化 toggleChapter**：移除了 scrollTo 逻辑和 nextTick import，因为章列表本身不再需要滚动

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: 移除 sticky、添加 verse-list 独立滚动、去掉经文间隙
