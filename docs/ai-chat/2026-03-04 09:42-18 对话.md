# 对话 18

**时间**: 2026-03-04 12:20

---

**用户**: 重新导入后还是有数字节问题，要求去掉文档自带数字，经文和节数字对齐

**Claude**: 发现策略0可能因 PDF 不可见字符（零宽空格等）而未生效。三重修复：
1. 清理不可见字符（\u200B-\u200D、\uFEFF、\u00A0）
2. 策略0正则改为更严格的 `/^(\d{1,3})$/`（先 trim 后匹配）
3. 新增 `_mergeStandaloneNumbers` 安全网函数：检测 [数字, 正文] 交替模式并合并
同时清除 Vite 缓存 (rm -rf node_modules/.vite) 并重启服务器。

**修改的文件**:
- `website/src/utils/fileImport.js`: 重写 splitTextToVerses（不可见字符清理 + 安全网合并函数）
