# 对话 5

**时间**: 2026-02-28 15:27

---

**用户**: 网页打开后是白屏的

**Claude**: 检查后发现是 vue-i18n 配置缺少 `legacy: false`，导致在 `<script setup>` 中使用 `useI18n()` 时报错。添加 `legacy: false` 后让用户刷新验证。

**修改的文件**:
- `website/src/i18n/index.js`: 添加 `legacy: false` 配置
