# 对话 1

**时间**: 2026-03-02 11:58

---

**用户**: 创建公开资源下载接口，供 APP 扫码下载公开资源，无需认证。

**Claude**: 创建了 PublicResourceResource.java 公开资源下载端点（路径 `/public/api/resource/download/{id}`），并在 ResourceService.java 中新增了 `exportPublicResource` 方法。该方法仅检查资源是否公开（isPublic==1），不需要用户身份验证。返回与现有私有导出接口相同的 JSON 格式（type + meta + books），带有 Content-Disposition 附件头。资源不存在返回 404，资源未公开返回 403。

**修改的文件**:
- `server/src/main/java/org/wxs/bibledictionary/resource/resource/PublicResourceResource.java`: 新建公开资源下载 REST 端点
- `server/src/main/java/org/wxs/bibledictionary/resource/service/ResourceService.java`: 新增 exportPublicResource 方法
