# 对话 127

**时间**: 2026-03-09 14:18

---

**用户**: 智能排版后存在标点符号错误（如"说：。"），希望节号和经文紧挨着。

**Claude**: 修复了两个问题：

1. **标点符号错误**：`restorePunctuation()` 的句尾标点检测正则遗漏了 `：`（冒号）、`，`（逗号）、`、`（顿号）等合法结尾标点，导致以这些标点结尾的经文被错误追加句号（如"说：。"）。修复：扩展正则，新增冗余标点组合清理（`：。` → `：`）。

2. **节号间距过大**：经典模式和大字模式的 `.verse-num` / `.verse-num-large` 原有 `min-width: 28px/40px` + `padding-right: 10px`，改为仅 `padding-right: 4px`，节号紧贴经文。

**修改的文件**:
- `website/src/utils/bibleVerseParser.js`: `restorePunctuation()` 扩展合法结尾标点；`normalizeText()` 新增冗余标点组合修复
- `website/src/views/BibleRead.vue`: `.verse-num` 和 `.verse-num-large` 移除 min-width 和 text-align，缩小 padding-right
