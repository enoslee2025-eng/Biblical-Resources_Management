# 对话 26

**时间**: 2026-03-04 14:33

---

**用户**: 希望经文内容和预览两个Tab合并，在预览页面中可以直接编辑修改，包括字体、经文编辑等

**Claude**: 将"经文内容"和"预览"两个 Tab 合并为单一的"经文编辑" Tab，实现所见即所得的编辑体验：

1. **双模式切换**：可视编辑（inline）和文本编辑（raw），右上角按钮切换
2. **可视编辑模式**：
   - 经文以预览格式展示（节号 + 格式化文本）
   - 点击任一节经文即可直接编辑
   - Enter 保存并跳到下一节，Escape 取消编辑
   - 每行悬停显示 + (插入) 和 × (删除) 按钮
   - 底部"添加新节"按钮
3. **字体设置工具栏**：
   - 字号调节（A- / A+，12-28px）
   - 行距滑块（1.2 - 3.0）
   - 字体选择（默认/宋体/楷体/黑体/等宽）
   - 实时节数统计
4. **文本编辑模式**：保留原有 textarea + 行号编辑器
5. 删除了独立的"预览" Tab

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 新增 editorViewMode、editingVerseIndex、fontSettings 状态；新增 currentVerses、verseAreaStyle 计算属性；新增内联编辑系列函数（startEditVerse、finishEditVerse、onVerseInput、finishAndNext、addVerseAfter、deleteVerse、addFirstVerse、syncVersesTextFromChapter）；重构模板合并两个 Tab；新增字体工具栏和可视编辑器 CSS
