# 对话 22

**时间**: 2026-03-04 13:56

---

**用户**: 向下滑动加载新章节时，顶部的章节选择器也应该同步更新显示当前可见的章号

**Claude**: 新增 `scrolledChapterOffset` 和 `displayChapterIndex` 机制：
- 滚动时统计已滚过顶部的章节分隔符数量，计算当前可见章
- 页面标题和章节选择器绑定 `displayChapterIndex`（= startChapter + scrollOffset）
- 用户手动选择章节时自动重置偏移量，回到单章模式
- 选择器改用 `:model-value` + `@update:model-value` 分离读写

**修改的文件**:
- `website/src/views/BibleRead.vue`: 新增 scrolledChapterOffset/displayChapterIndex；更新 onVerseAreaScroll 检测可见章；pageTitle 使用 displayChapterIndex；选择器绑定改为 displayChapterIndex
