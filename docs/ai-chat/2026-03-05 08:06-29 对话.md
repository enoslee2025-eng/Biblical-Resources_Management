# 对话 29

**时间**: 2026-03-05 11:45

---

**用户**: 「阅览」按钮不是我想要的（router.back 不对）

**Claude**: 将「阅览」按钮从 `router.back()` 改为 `router.push('/bible/read/${resourceId}')`，直接跳转到当前资源的阅读页面。同时添加 `v-if="isEdit"` 条件，只在编辑已有资源时显示（新建时没有资源 ID 无法跳转）。

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 「阅览」按钮改为跳转阅读页，添加 `v-if="isEdit"`
