# 对话 24

**时间**: 2026-03-17 15:57

---

**用户**: 确认注释模块重新设计方案（ok）

**Claude**: 完成注释模块从逐节模式到段落/主题模式的全面重构：

1. **fileImport.js**: parseCommentaryText 改为按段落智能分割+标题提取（不再按经文引用切分）
2. **CommentaryDetail.vue**: 详情页显示目录大纲和段落统计（不再显示66卷覆盖率）
3. **CommentaryRead.vue**: 段落式阅读，左侧目录导航+上下翻页（不再逐节显示）
4. **CommentaryEditor.vue**: 编辑条目改为标题+内容（不再需要经文引用字段）
5. **ImportDialog.vue**: 更新注释说明注释
6. **i18n**: 8个语言文件全部添加新的注释模块翻译key

数据格式从 `[{verse, content}]` 改为 `[{title, content}]`，兼容旧格式自动转换。

**修改的文件**:
- `website/src/utils/fileImport.js`: parseCommentaryText 改为段落智能分割
- `website/src/views/CommentaryDetail.vue`: 段落目录大纲替代书卷覆盖率
- `website/src/views/CommentaryRead.vue`: 段落式阅读+目录导航
- `website/src/views/CommentaryEditor.vue`: 标题+内容替代经文引用+内容
- `website/src/components/ImportDialog.vue`: 更新注释解析说明
- `website/src/i18n/*.js`: 8个语言文件新增注释段落模式的翻译key
