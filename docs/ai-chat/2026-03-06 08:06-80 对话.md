# 对话 80

**时间**: 2026-03-06 11:40

---

**用户**: 关闭章后应回到起初位置，不要因为滑动经文后关闭章就跑到其他位置。章和"经文解析结果"标题之间有缝隙会漏出文字，不好看。

**Claude**: 修复了两个问题：

1. **关闭时恢复滚动位置**：展开章前记录 `savedScrollTop`，关闭时用 `scrollTo` 恢复到展开前的位置
2. **消除缝隙**：`.parsed-panel` 的 `padding: 8px 0` 改为 `padding: 0`，消除章标题与面板头之间的间隙
3. **sticky 标题添加底部阴影**：`box-shadow` + `border-bottom` 防止经文文字从标题底部透出来

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: toggleChapter 添加 savedScrollTop 保存/恢复逻辑，parsed-panel padding 改为 0，sticky header 添加 box-shadow
