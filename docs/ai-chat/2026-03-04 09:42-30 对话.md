# 对话 30

**时间**: 2026-03-04 14:52

---

**用户**: 行号1-21有灰色背景、22-32没有，不统一；第26节"创造人类"是小标题不是经文，识别功能需要提升

**Claude**: 两项修复：

1. **行号背景统一**：问题是 `.line-numbers` div 高度可能不够覆盖全部行。改为在 `.verses-editor-wrapper` 上用 `linear-gradient(to right, #f5f7fa 36px, #fff 36px)` 做固定灰色条带，确保无论内容多长背景都一致

2. **小标题过滤增强**：
   - 新增 `_stripLeadingHeadings()` 函数：移除经文开头混入的短标题（≤10字、无句尾标点）
   - 新增 `_filterHeadingVerses()` 函数：最终过滤被错误识别为经文的小标题行（前后文是正常经文则判定为标题并移除）
   - 在策略0、策略1、`_mergeStandaloneNumbers` 中都应用了过滤

**修改的文件**:
- `website/src/views/BibleEditor.vue`: wrapper 背景改用 linear-gradient；line-numbers 背景改为 transparent
- `website/src/utils/fileImport.js`: 新增 _stripLeadingHeadings、_filterHeadingVerses 函数；策略0/1及 _mergeStandaloneNumbers 中应用标题过滤
