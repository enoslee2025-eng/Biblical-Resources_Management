# 对话 155

**时间**: 2026-03-10 10:04

---

**用户**: 统计数字字体还是衬线体，要改成正常字体

**Claude**: 用 `!important` 强制将 `.stat-number` 字体改为系统无衬线体，避免被其他样式覆盖。

**修改的文件**:
- `website/src/views/DataCenter.vue`: `.stat-number` 字体强制使用系统无衬线体
