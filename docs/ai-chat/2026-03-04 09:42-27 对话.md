# 对话 27

**时间**: 2026-03-04 14:40

---

**用户**: 进度条不要两行，放一行精简些；文本编辑模式 textarea 只显示15行遮挡了后续经文，需要全部呈现并且行号对齐同步滚动

**Claude**: 两个修复：
1. 进度条从两行合并为单行精简布局（flex 横排：书卷进度 + 章节进度 + 跳转按钮）
2. 文本编辑器：textarea 从固定 rows=15 改为 autosize 自动撑高；行号用 rawLineCount（含空行）精确匹配行数；wrapper 改为 max-height: 65vh + overflow-y: auto 整体滚动，行号和文本自然同步

**修改的文件**:
- `website/src/views/BibleEditor.vue`: 新增 rawLineCount 计算属性；进度条改为 .progress-compact 单行布局；textarea 改用 :autosize；行号改用 rawLineCount；wrapper 改为整体滚动；更新相关 CSS
