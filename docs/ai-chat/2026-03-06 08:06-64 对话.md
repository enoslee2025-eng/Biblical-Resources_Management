# 对话 64

**时间**: 2026-03-06 10:07

---

**用户**: 经文编辑器的自动排版不好，文本没有一行一行分开，标点符号也不是中文的

**Claude**: 修复了三个问题：

1. **节号重复BUG**：`formatVersesForApp` 函数拼接经文时，`v.text` 已包含节号（如 "1经文"），又拼上 `v.verse`（1），导致 "11经文"，完全破坏节号识别。修复后先去掉文本开头的重复节号再拼接
2. **标点符号转换不全**：`fixPunctuation` 函数新增全角英文标点（．，；：？！）→ 中文标点的转换，英文句号转换改为更精确的前后文判断（中文字符前后的句号才转换，避免误改小数点），添加连续重复标点修复
3. **经文行间显示**：富文本编辑器的 `<p>` 标签增加底部边框和间距，使每节经文之间有更清晰的视觉分隔；自动排版后添加 `nextTick` 确保编辑器正确刷新

**修改的文件**:
- `website/src/utils/bibleFormat.js`: 修复 formatVersesForApp 节号重复、增强 fixPunctuation 标点转换、result.text 保留节号前缀
- `website/src/views/BibleEditor.vue`: autoFormatChapter 添加 nextTick 等待响应式更新
- `website/src/components/RichTextEditor.vue`: 段落增加底部边框和间距、经文节号上标样式
