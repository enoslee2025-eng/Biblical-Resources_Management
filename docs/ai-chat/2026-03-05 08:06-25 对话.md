# 对话 25

**时间**: 2026-03-05 11:25

---

**用户**: 放在跟书卷、章节一行，然后均衡排列

**Claude**: 将两行布局合并回一行。所有控件（书卷/章节选择、上一章/下一章、模式/字体/对照/修改）在同一行，中间用 `.control-spacer`（flex: 1）弹性间距分隔，左侧紧凑排列，右侧按钮均匀分布。

**修改的文件**:
- `website/src/views/BibleRead.vue`:
  - 模板：移除 `.control-row-top` 和 `.control-row-bottom` 包裹 div，所有控件直接放在 `.control-bar` 内，中间插入 `.control-spacer`
  - CSS：`.control-bar` 改回单行 flex，新增 `.control-spacer { flex: 1 }`，移除 `.control-row-top` 和 `.control-row-bottom`
