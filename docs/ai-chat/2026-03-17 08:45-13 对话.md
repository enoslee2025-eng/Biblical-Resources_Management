# 对话 13

**时间**: 2026-03-17 10:58

---

**用户**: .doc 导入还是同样的错误

**Claude**: 找到了真正的根本原因：上传 .doc 文件时，代码手动设置了 `Content-Type: multipart/form-data`，但这样会覆盖浏览器自动生成的包含 boundary 分隔符的头信息，导致后端无法解析上传的文件。修复方法是删掉这个多余的设置，让浏览器自动处理。

**修改的文件**:
- `website/src/api/resource.js`: 移除手动设置的 Content-Type 头
