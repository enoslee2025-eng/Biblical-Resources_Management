# 对话 78

**时间**: 2026-03-06 11:37

---

**用户**: 章标题没有固定住，滑动经文时章需要一直显示在顶部

**Claude**: 改为只对当前展开的章标题应用 sticky 定位（之前全部去掉了 sticky，导致章标题也跟着滚走）：

1. 章标题添加条件 class：`:class="{ 'chapter-header-sticky': expandedChapter === idx }"`
2. 新增 `.chapter-header-sticky` 样式：`position: sticky; top: 0; z-index: 10`
3. 未展开的章标题不带 sticky，不会出现多个标题堆叠的问题
4. 恢复 verse-list 为自然流式布局（移除 max-height）

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: 条件 sticky class、新增 .chapter-header-sticky 样式、移除 verse-list max-height
