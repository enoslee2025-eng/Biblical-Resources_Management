# 对话 81

**时间**: 2026-03-06 11:44

---

**用户**: 经文换行时续行没有对齐正文（Hanging Indent），要求使用两列布局：左列节号固定占位，右列正文自动换行续行对齐。

**Claude**: 将 `.verse-item` 改为 CSS Grid 两列布局：

- `display: grid; grid-template-columns: auto 1fr; column-gap: 6px; align-items: baseline`
- `.verse-num`：右对齐，固定最小宽度 24px，节号始终在左列
- `.verse-text`：占满右列，自动换行时续行自然与首行对齐

**修改的文件**:
- `website/src/views/BibleImportPreview.vue`: verse-item 改为 grid 布局，verse-num 右对齐固定宽度
