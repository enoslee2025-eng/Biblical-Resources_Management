# 对话 50

**时间**: 2026-03-05 12:38

---

**用户**: 编辑器中节号数字重复显示（如 11起初、22地是、555神称光...）

**Claude**: 原因是经文文本 `v.text` 已包含节号数字，加载时 `<sup>` 又添加了一次，每次保存加载循环都会多一位。修复：加载时用正则去掉 `v.text` 开头已有的节号数字。

**修改的文件**:
- `website/src/views/BibleEditor.vue`:
  - `loadRawRichFromChapter`: 添加 cleanText 去除开头节号
  - `loadRichTextFromChapter`: 同样添加 cleanText 去除开头节号
